import 'package:flutter/material.dart';

const double defaultPadding = 30.0;
const double defaultPaddingXs = 5.0;
const double defaultPaddingSm = 10.0;
const double defaultPaddingMd = 20.0;

const double defaultColumnSpacingSm = 10.0;
const double defaultColumnSpacingXs = 5.0;
const double defaultColumnSpacingMd = 15.0;
const double defaultColumnSpacing = 20.0;
const double defaultColumnSpacingLg = 25.0;
const double defaultColumnSpacingXXL = 35.0;

const double defaultAppBarHeight = 80.0;
const Radius bottomSheetRadius = Radius.circular(30);
const Radius inputBorderRadius = Radius.circular(5.0);
BorderRadius sendButtonRadius = BorderRadius.circular(50.0);
const Radius messageBorderRadius = Radius.elliptical(20, 40);
const double userDpRadius = 20.0;
const BorderRadius cardRadius = BorderRadius.all(Radius.circular(15));
const BorderRadius bottomBarRadius = BorderRadius.all(Radius.circular(10));
const BorderRadius bottomStackBar = BorderRadius.only(bottomLeft:  Radius.elliptical(500.0,100.0), bottomRight: Radius.elliptical(500.0, 100.0));

BorderRadius elevatedButtonRadius = BorderRadius.circular(5.0);
EdgeInsetsDirectional elevatedPaddingButtonPrimary = const EdgeInsetsDirectional.symmetric(vertical: 20.0);

const double inputIconsSize = 25.0;